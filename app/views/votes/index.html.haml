%h1 Listing votes

%table
  %tr
    %th Vote belongs to all
    %th Vote belongs to profesors
    %th Vote time
    %th
    %th
    %th

  - @votes.each do |vote|
    %tr
      %td= vote.vote_belongs_to_all
      %td= vote.vote_belongs_to_profesors
      %td= vote.vote_time
      %td= link_to 'Show', vote
      %td= link_to 'Edit', edit_vote_path(vote)
      %td= link_to 'Destroy', vote, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Vote', new_vote_path
